{
  "name": "yaf-extension",
  "repository": {
    "type": "git",
    "url": "https://github.com/falsefalse/yaf-extension.git"
  },
  "version": "1.1.6",
  "private": true,
  "scripts": {
    "_stash_lint_clean": "git stash -q && yarn -s lint && jake -q clean",
    "release:chrome": "yarn -s _stash_lint_clean && jake -q && open ./pkg",
    "release:firefox": "yarn -s _stash_lint_clean && jake -q firefox && open ./pkg",
    "ch": "DEV=1 jake -q",
    "ff": "DEV=1 jake -q firefox",
    "tc": "tsc --noEmit",
    "lint": "yarn -s format & p=$! ; yarn -s eslint ||s=1 ; wait $p||s=1 ; exit $s",
    "eslint": "eslint --fix *.js **/*.ts **/*.ejs.html",
    "format": "p=.prettierignore c=$(<$p) ; cat .gitignore >> $p ; prettier -u --list-different . ||s=1 ; echo \"$c\" > $p ; exit $s",
  },
  "devDependencies": {
    "@types/chrome": "^0.0.227",
    "@types/firefox-webext-browser": "^111.0.1",
    "@types/offscreencanvas": "^2019.7.0",
    "@typescript-eslint/eslint-plugin": "^5.57.0",
    "@typescript-eslint/parser": "^5.57.0",
    "eslint": "^8.32.0",
    "eslint-config-prettier": "^8.6.0",
    "eslint-plugin-lodash-template": "^0.21.0",
    "lodash.template": "^4.5.0",
    "prettier": "^2.8.3",
    "typescript": "^5.0.3",
    "uglify-js": "^3.17.4"
  }
}
